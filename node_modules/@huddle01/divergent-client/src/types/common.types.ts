export interface IMessage {
  to: string;
  from: string;
  message: string;
  timestamp: string;
  verner?: {
    keys: {
      [participant: string]: string;
    };
    nonce: string;
  };
}

export interface IUserInfo {
  uid: string;
  socket: string;
  node: string;
  avatarUrl: string;
  messages: Array<[]>;
}
