import { createContext } from "react";

import DivergentAVClient from "./DivergentAVClient";

export const DivergentAVClientContext = createContext<DivergentAVClient>(
  {} as DivergentAVClient
);

// export const DivergentClientStoreContext = createContext<HuddleClientStore>(initialStore);
