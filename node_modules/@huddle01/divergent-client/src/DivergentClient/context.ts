import { createContext } from 'react';

import DivergentClient from './DivergentClient';

export const DivergentClientContext = createContext<DivergentClient>({} as DivergentClient);

// export const DivergentClientStoreContext = createContext<HuddleClientStore>(initialStore);
